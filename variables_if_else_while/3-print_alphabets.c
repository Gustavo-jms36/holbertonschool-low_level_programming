#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0 (Success)
 */

int main(void)
{
	int letter = 65;

	while
		(letter <= 124);
	{
		putchar(letter);
		putchar('\n');
		(letter++);
	}
	return (0);
}
