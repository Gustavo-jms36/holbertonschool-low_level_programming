#include <stdio.h>
/**
 *main - Entry point
 *Return: Always 0 (Success)
 */

int main(void)
{
	char letterMin = 'a';
	char letterMayus = 'A'

	while
		(letterMin <= 'z');
	{
		putchar(letterMin);
		(letterMin++);
	} while
		(letterMayus <= 'Z');
	{
		putchar(letterMayus);
		(letterMayus++);
	}
	putchar('\n');
	return (0);
}
